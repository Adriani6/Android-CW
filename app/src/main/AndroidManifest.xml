<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.a1502689.adriani6.cw">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Main" />
        <activity android:name=".MessageController" />

        <activity android:name=".Home" android:label="@string/HomeLabel"></activity>
        <activity            android:name=".Edit"            android:label="@string/EditLabel"></activity>
        <activity            android:name=".Messages"            android:label="@string/MessagesLabel"></activity>
    </application>

</manifest>